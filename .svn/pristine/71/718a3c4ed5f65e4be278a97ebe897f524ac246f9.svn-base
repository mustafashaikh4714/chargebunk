import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChargepointconfigurationviewComponent } from './chargepointconfigurationview.component';

describe('ChargepointconfigurationviewComponent', () => {
  let component: ChargepointconfigurationviewComponent;
  let fixture: ComponentFixture<ChargepointconfigurationviewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ChargepointconfigurationviewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChargepointconfigurationviewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
